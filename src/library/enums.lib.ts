export enum UserRole {
    user = 'user',
    courier = 'courier',
    admin = 'admin'
}

export enum OrderStatus {
    waiting = 'waiting',
    inProcess = 'inProcess',
    inDistribution = 'inDistribution',
    completed = 'completed'
}